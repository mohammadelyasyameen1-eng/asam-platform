"use client";
import Link from "next/link";import { useState } from "react";import { createClient } from "@/lib/supabase/client";import { ArrowRight, UserRoundPlus } from "lucide-react";
export default function Register(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [name,setName]=useState("");const [error,setError]=useState("");const [done,setDone]=useState(false);const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");setBusy(true);try{const {error}=await createClient().auth.signUp({email,password,options:{data:{full_name:name}}});if(error)throw error;setDone(true)}catch(err){setError(err instanceof Error?err.message:"Unable to create your account.")}finally{setBusy(false)}}return <section className="auth-page" data-landmark="register"><div className="auth-card card"><div className="auth-icon"><UserRoundPlus/></div><span className="eyebrow">Create an account</span><h1>Start your ASAM journey.</h1><p>Your account lets you submit and follow a membership application.</p>{done?<div className="register-done"><h2>Check your email</h2><p>Use the confirmation link to activate your account, then sign in to continue your application.</p><Link className="button button-dark" href="/login">Go to sign in <ArrowRight size={15}/></Link></div>:<form onSubmit={submit}><label>Full name<input required maxLength={120} value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></label><label>Email address<input type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required minLength={10} autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/><small>Use at least 10 characters.</small></label>{error&&<div className="form-error" role="alert">{error}</div>}<button className="button button-dark auth-submit" disabled={busy}>{busy?"Creating…":"Create account"}<ArrowRight size={16}/></button><p className="fine-print">ASAM may require email confirmation before you can sign in.</p></form>}<div className="auth-foot">Already registered? <Link href="/login">Sign in</Link></div></div></section>}
