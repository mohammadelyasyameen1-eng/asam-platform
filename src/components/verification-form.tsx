"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import { ArrowRight,Search } from "lucide-react";
export function VerificationForm({kind}:{kind:"member"|"certificate"}){const [token,setToken]=useState("");const router=useRouter();function submit(e:React.FormEvent){e.preventDefault();if(/^[0-9a-f-]{36}$/i.test(token.trim()))router.push(`${kind==="member"?"/verify":"/certificates/verify"}/${token.trim()}`)}return <section className="verify-result-page" data-landmark={kind==="member"?"member-verify":"certificate-verify"}><div className="verify-result card"><div className="verify-result-icon"><Search/></div><span className="eyebrow">Credential check</span><h1>Verify a {kind==="member"?"member ID":"certificate"}.</h1><p>Enter the unique verification code shown on the credential. We only return basic validity information.</p><form onSubmit={submit} className="verify-form"><label htmlFor="verify-token">Verification code</label><input id="verify-token" required minLength={36} maxLength={36} value={token} onChange={e=>setToken(e.target.value)} placeholder="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"/><button className="button button-dark">Check credential <ArrowRight size={15}/></button></form><small>Codes are unique and contain no personal information.</small></div></section>}
