"use client";
import Link from "next/link";import { useState } from "react";import { Menu, X, ArrowUpRight } from "lucide-react";
const links=[["About","/about"],["Leadership","/leadership"],["Chapters","/chapters"],["Events","/events"],["News","/news"],["Membership","/membership"],["Contact","/contact"]];
export function SiteHeader(){const [open,setOpen]=useState(false);return <header className="site-header"><div className="container header-inner"><Link href="/" className="brand" aria-label="ASAM home"><img className="brand-mark" src="/images/brand/asam-emblem.png" alt=""/><span><strong>ASAM</strong><small>Afghan Students Association<br/>in Malaysia</small></span></Link><nav className="desktop-nav" aria-label="Main navigation">{links.map(([n,h])=><Link key={n} href={h}>{n}</Link>)}</nav><div className="header-actions"><Link className="login-link" href="/login">Log in</Link><Link className="button button-dark header-join" href="/membership">Become a member <ArrowUpRight size={15}/></Link><button className="menu-toggle" type="button" aria-label={open?"Close menu":"Open menu"} aria-expanded={open} aria-controls="asam-mobile-navigation" onClick={()=>setOpen(!open)}>{open?<X size={21}/>:<Menu size={21}/>}</button></div></div>{open&&<nav id="asam-mobile-navigation" className="mobile-nav" aria-label="Mobile navigation">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} key={n} href={h}>{n}</Link>)}<Link onClick={()=>setOpen(false)} href="/login">Member log in</Link></nav>}</header>}
